# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 lituus-lab
## UniColor — perceptual color engine. Umbrella module: re-exports every public
## submodule as the domain lands across the following PRs. For now it carries the
## version, so the C ABI and Python binding have something real to expose.
const UniColorVersion* = "0.1.0"
